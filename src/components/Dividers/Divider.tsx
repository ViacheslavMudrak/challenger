import { DividerBase, DividerProps } from './Divider.base';
import { Variant } from './Divider.types';

export const Divider1Top = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider1Top;

  return <DividerBase {...props} />;
};

export const Divider1Bottom = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider1Bottom;

  return <DividerBase {...props} />;
};

export const Divider2Top = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider2Top;

  return <DividerBase {...props} />;
};

export const Divider2Bottom = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider2Bottom;

  return <DividerBase {...props} />;
};

export const Divider3Top = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider3Top;

  return <DividerBase {...props} />;
};

export const Divider3Bottom = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider3Bottom;

  return <DividerBase {...props} />;
};

export const Divider4Top = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider4Top;

  return <DividerBase {...props} />;
};

export const Divider4Bottom = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider4Bottom;

  return <DividerBase {...props} />;
};

export const Divider5Top = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider5Top;

  return <DividerBase {...props} />;
};

export const Divider5Bottom = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider5Bottom;

  return <DividerBase {...props} />;
};

export const Divider6Top = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider6Top;

  return <DividerBase {...props} />;
};

export const Divider6Bottom = (props: DividerProps) => {
  props.rendering.params.FieldNames = Variant.Divider6Bottom;

  return <DividerBase {...props} />;
};

export const Default = (props: DividerProps) => {
  return <DividerBase {...props} />;
};
