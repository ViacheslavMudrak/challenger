// import { render, screen } from '@testing-library/react';
// import DataNav from '../mocks/nav.json';
// import Nav from '../Nav';
// import { NavProps } from '../Nav.types';

//TODO: Fix me due to error -> cannot use import statement outside of module
describe('<Nav />', () => {
  // test.skip('should render correctly', async () => {
  //   const { rendering } = DataNav as unknown as NavProps;

  //   render(<Nav rendering={rendering} />);

  //   const nav = await screen.findByRole('navigation');

  //   expect(nav).toBeInTheDocument();
  // });

  test('skip for now due to "cannot use import statement outside of module" error', () => {
    expect(true).toBeTruthy();
  });
});
